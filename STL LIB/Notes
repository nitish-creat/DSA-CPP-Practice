/*
STL CONTAINER 
VECTOR --> DYNAMIC ARRAY
DEQUEUE --> DOUBLE ENDED QUEUE
LIST --> LINKED LIST, DOUBLY LINKED LIST 
ARRAY --> FIXED SIZED ARRAY
ASSOCIATIVE CONTAINER -->
    1. SET (UNNIQUE SORTED KEYS)
    2. MULTI SET (DUPLICATE KEYS ALLOWED)
    3. MAP (KEY, VALUE PAIRS)
    4. MULTI MAP (DUPLCATE KEYS)
UNORDERED CONTAINERS -->
    1. UNORDERED_SET
    2. UNORDERED_MAP (HASH MAP)
CONTAINER ADAPTERS -->
    1. STACK 
    2. QUEUE 
    3. PRIORITY_QUEUE


ITERATORS --> IT IS AN OBJECT THAT ACT LIKE A POINTER TO ACCESS CONTAINER ELEMENTS
ITERATOR FUNCTIONS -->
    1. BEGIN
    2. END
    3. RBEGIN (REVERSE BEGIN)
    4. REND (REVERSE END)

    TYPES :-
    INPUT ( READ ONLY MECHANISM )
    OUTPUT ( WRITE ONLY)
    FORWARD ( ++ OPERATOR )
    BIDIRECTIONAL OPERATOR ( ++ AND -- OPERATOR )
    RANDOM ACCESS

ITERATOR IS STL ABSTRACTION
IT IS SAFER THAN POINTERS 
IT IS WORK WITH CONTAINERS 
IT WORKS WITH ONLY ARRAYS 


*/